# Proposal: Compile, Commit, Push, and Deploy ElitePass Identity

## Business Intent
Compile the current codebase of `elitepass-identity`, commit all outstanding modified and untracked files, bump the package version to `1.0.1`, push the changes to GitHub `origin/main`, and deploy it by restarting the process in PM2.
